diff --git a/frontend/src/components/AgentEditor/AgentEditor.test.tsx b/frontend/src/components/AgentEditor/AgentEditor.test.tsx
index 42a6a5b..3acfd73 100644
--- a/frontend/src/components/AgentEditor/AgentEditor.test.tsx
+++ b/frontend/src/components/AgentEditor/AgentEditor.test.tsx
@@ -15,6 +15,7 @@ import {
   SHARED_CONTEXT_FILE_PLACEHOLDER,
 } from "@/types/config";
 import { useTools } from "@/hooks/useTools";
+import { toast } from "@/components/ui/toaster";
 
 // Mock the store
 vi.mock("@/store/configStore", () => ({
@@ -750,7 +751,7 @@ describe("AgentEditor", () => {
     expect(saveButton).toBeDisabled();
   });
 
-  it("handles delete button click with confirmation", () => {
+  it("persists confirmed deletion before the editor disappears", async () => {
     const confirmSpy = vi.spyOn(window, "confirm").mockReturnValue(true);
 
     render(<AgentEditor />);
@@ -759,9 +760,14 @@ describe("AgentEditor", () => {
     fireEvent.click(deleteButton);
 
     expect(confirmSpy).toHaveBeenCalledWith(
-      "Are you sure you want to delete this agent?",
+      "Delete this agent and save all pending configuration changes?",
     );
     expect(mockStore.deleteAgent).toHaveBeenCalledWith("test_agent");
+    await waitFor(() => expect(mockStore.saveConfig).toHaveBeenCalledOnce());
+    expect(mockStore.deleteAgent.mock.invocationCallOrder[0]).toBeLessThan(
+      mockStore.saveConfig.mock.invocationCallOrder[0],
+    );
+    expect(toast).not.toHaveBeenCalled();
 
     confirmSpy.mockRestore();
   });
@@ -775,7 +781,38 @@ describe("AgentEditor", () => {
     fireEvent.click(deleteButton);
 
     expect(mockStore.deleteAgent).not.toHaveBeenCalled();
+    expect(mockStore.saveConfig).not.toHaveBeenCalled();
+
+    confirmSpy.mockRestore();
+  });
+
+  it("reports a conflicting deletion save instead of silently losing it", async () => {
+    const confirmSpy = vi.spyOn(window, "confirm").mockReturnValue(true);
+    mockStore.saveConfig.mockResolvedValueOnce({
+      status: "error",
+      message: "Configuration changed elsewhere. Your draft has not been saved.",
+      diagnostics: [],
+    });
+    render(<AgentEditor />);
+    fireEvent.click(screen.getByRole("button", { name: /delete/i }));
+    await waitFor(() => expect(toast).toHaveBeenCalledWith({
+      title: "Delete Failed",
+      description: "Configuration changed elsewhere. Your draft has not been saved.",
+      variant: "destructive",
+    }));
+    confirmSpy.mockRestore();
+  });
 
+  it("reports unexpected deletion save failures", async () => {
+    const confirmSpy = vi.spyOn(window, "confirm").mockReturnValue(true);
+    mockStore.saveConfig.mockRejectedValueOnce(new Error("Network unavailable"));
+    render(<AgentEditor />);
+    fireEvent.click(screen.getByRole("button", { name: /delete/i }));
+    await waitFor(() => expect(toast).toHaveBeenCalledWith({
+      title: "Delete Failed",
+      description: "Network unavailable",
+      variant: "destructive",
+    }));
     confirmSpy.mockRestore();
   });
 
diff --git a/frontend/src/components/AgentEditor/AgentEditor.tsx b/frontend/src/components/AgentEditor/AgentEditor.tsx
index 0a0c7da..03dc66f 100644
--- a/frontend/src/components/AgentEditor/AgentEditor.tsx
+++ b/frontend/src/components/AgentEditor/AgentEditor.tsx
@@ -5,6 +5,8 @@ import { Button } from "@/components/ui/button";
 import { Input } from "@/components/ui/input";
 import { Textarea } from "@/components/ui/textarea";
 import { Checkbox } from "@/components/ui/checkbox";
+import { toast } from "@/components/ui/toaster";
+import { showSaveFailureToastIfNeeded } from "@/components/shared/EditorPanel";
 import {
   Select,
   SelectContent,
@@ -369,12 +371,24 @@ export function AgentEditor() {
     [handleFieldChange, toolInfoByName],
   );
 
-  const handleDelete = () => {
+  const handleDelete = async () => {
     if (
+      !isLoading &&
       selectedAgentId &&
-      confirm("Are you sure you want to delete this agent?")
+      confirm("Delete this agent and save all pending configuration changes?")
     ) {
       deleteAgent(selectedAgentId);
+      try {
+        const result = await saveConfig();
+        showSaveFailureToastIfNeeded(result, { title: "Delete Failed" });
+      } catch (error) {
+        toast({
+          title: "Delete Failed",
+          description:
+            error instanceof Error ? error.message : "Failed to save deletion.",
+          variant: "destructive",
+        });
+      }
     }
   };
 
@@ -568,6 +582,7 @@ export function AgentEditor() {
       onSave={handleSave}
       onDelete={handleDelete}
       disableSave={isLoading}
+      disableDelete={isLoading}
       onBack={() => selectAgent(null)}
     >
       {agentRootError && (
